import styled from "styled-components"

export const AddPostPage = styled.div`
  margin-top: 40px;
  min-height: 100vh;
  .text-editor {
    background-color: white;
  }
  .ql-editor {
    height: 300px;
  }
  .form-label {
    font-size: 1em;
    color: var(--widget-title-color);
    font-weight: 600;
  }
  .form-file-label {
    font-size: 1em;
    color: var(--widget-title-color);
    font-weight: 600;
  }
  .form-control-file {
    color: var(--widget-title-color);
  }
  .quill {
    background-color: white;
  }
`
